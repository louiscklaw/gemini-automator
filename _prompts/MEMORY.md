# MEMORY

- when you find yourself cannot complete the task in one session. write the remaining item(s)/task(s) here.
- on starting use of the framework(s). always start from the scaffolding tools of the framework. for example. use `create-react-app` for react source code testing.
- `restart: on-failure` for any container in this project.
- rebuild docker container(s) after operation when necessary by running `002_docker/dc_up.sh`
- delegrate to related agents if needed.

## AGENT 003_client

- run `npm run tidy` after code work

## AGENT 006_server

- run `npm run tidy` after code work

## AGENT 007_testing

- run `npm run tidy` after code work
