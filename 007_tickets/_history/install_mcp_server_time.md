# install mcp_server_time

please fetch the steps from `https://raw.githubusercontent.com/modelcontextprotocol/servers/refs/heads/main/src/time/README.md` and install the `mcp-server-time` to `Dockerfile.opencode_ob`

and then test building the image using `cd 002_docker; sudo ./dc_test_build.sh`

## Expected result

- an docker image built.
