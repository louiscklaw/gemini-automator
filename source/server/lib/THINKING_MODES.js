/**
 * THINKING_MODES.js
 * Constants defining the available thinking mode options in Gemini.
 *
 * - 'Fast': Quick response mode, suitable for simple queries.
 * - 'Thinking': Deeper reasoning mode for complex questions.
 */

/** Fast response mode — used for quick, simple queries */
const THINKING_MODE_FAST = 'Fast';
exports.THINKING_MODE_FAST = THINKING_MODE_FAST;

/** Thinking (deep reasoning) mode — used for complex queries */
const THINKING_MODE_THINKING = 'Thinking';
exports.THINKING_MODE_THINKING = THINKING_MODE_THINKING;
